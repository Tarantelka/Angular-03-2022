import { DescriptionSortenerPipe } from './description-sortener.pipe';

describe('DescriptionSortenerPipe', () => {
  it('create an instance', () => {
    const pipe = new DescriptionSortenerPipe();
    expect(pipe).toBeTruthy();
  });
});
