import { ThousandSeperatorPipe } from './thousand-seperator.pipe';

describe('ThousandSeperatorPipe', () => {
  it('create an instance', () => {
    const pipe = new ThousandSeperatorPipe();
    expect(pipe).toBeTruthy();
  });
});
